<section id="about" class="about reveal">
  <div class="container">

    <div class="about-left reveal-item reveal-delay-1">
      <div class="section-index">
        <span>02</span>
        <span class="line"></span>
        <p>À propos</p>
      </div>

      <h2>
        Curieuse.<br />
        Rigoureuse.<br />
        Toujours en apprentissage.
      </h2>
    </div>

    <div class="about-right">

      <div class="about-intro">
        <div class="about-copy reveal-item reveal-delay-2">

      <p class="lead">
        Kaoutar BENDIDI, étudiante en 4e année d’ingénierie informatique. Je m’intéresse au développement Full Stack et j’explore progressivement le cloud, le DevOps et les usages de l’IA dans le développement logiciel.
      </p>

      <p>
        À travers mes projets, j’apprends à concevoir une application dans son ensemble : interface, logique backend, APIs et bases de données. J’aime comprendre comment les différentes parties d’un produit fonctionnent ensemble et améliorer progressivement ma façon de coder.
      </p>

        </div>

        <figure class="about-photo reveal-item reveal-delay-3">
          <img
            src="/kaoutar-profile.jpg"
            alt="Portrait de Kaoutar Bendidi"
            loading="lazy"
          />
        </figure>
      </div>

    </div>

    <div class="about-meta reveal-item reveal-delay-4">
        <div>
          <span>01</span>
          <h3>Frontend</h3>
          <p>Interfaces modernes, responsive et accessibles.</p>
        </div>

        <div>
          <span>02</span>
          <h3>Backend</h3>
          <p>APIs, logique applicative et gestion des données.</p>
        </div>

        <div>
          <span>03</span>
          <h3>Approche produit</h3>
          <p>Comprendre le besoin avant de construire la solution.</p>
        </div>
      </div>

  </div>
</section>

<style>
  .about {
    padding: 130px 0;
  }

  .container {
    width: min(1120px, 90%);
    margin: 0 auto;

    display: grid;
    grid-template-columns: 0.72fr 1.28fr;
    column-gap: 70px;
    row-gap: 70px;

    align-items: start;
  }

  .section-index {
    display: flex;
    align-items: center;
    gap: 12px;

    margin-bottom: 28px;
  }

  .section-index span:first-child {
    font-family: Georgia, serif;
    color: var(--sage-dark);
  }

  .line {
    width: 42px;
    height: 1px;

    background: var(--sage);
  }

  .section-index p {
    margin: 0;

    text-transform: uppercase;
    letter-spacing: 3px;

    color: var(--mauve);

    font-size: 0.72rem;
    font-weight: 700;
  }

  h2 {
    margin: 0;

    font-family: Georgia, serif;
    font-size: clamp(3rem, 6vw, 5.4rem);
    line-height: 0.95;

    letter-spacing: -2px;

    color: var(--mauve-dark);
  }

  .about-right {
    padding-top: 44px;
  }

  .lead {
    font-family: Georgia, serif;

    font-size: clamp(1.5rem, 2.5vw, 2.2rem);
    line-height: 1.4;

    color: var(--text);

    margin-top: 0;
  }

  .about-right {
  padding-top: 44px;
}

.lead {
  font-family: Georgia, serif;
  font-size: clamp(1.5rem, 2.2vw, 2rem);
  line-height: 1.45;
  color: var(--text);
  margin-top: 0;
  margin-bottom: 22px;
}

.about-intro {
  display: grid;
  grid-template-columns: minmax(320px, 1fr) clamp(300px, 32vw, 360px);
  gap: 28px;
  align-items: start;
}

.about-copy > p:not(.lead) {
  max-width: 620px;
  color: var(--text-soft);
  font-size: 1rem;
  line-height: 1.8;
  margin-bottom: 0;
}

.about-photo {
  margin: 0;
}

.about-photo img {
  display: block;
  width: 100%;
  aspect-ratio: 4 / 5;
  object-fit: cover;
  object-position: center;
  border-radius: 20px;
}

.about-meta {
  grid-column: 1 / -1;
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 28px;
  border-top: 1px solid var(--border);
}

.about-meta > div {
  display: grid;
  grid-template-columns: 45px 1fr;
  align-items: start;
  column-gap: 18px;
  row-gap: 8px;

  padding: 22px 0;

  border-bottom: 1px solid var(--border);

  transition:
    transform 0.25s ease,
    padding-left 0.25s ease;
}

.about-meta > div:hover {
  padding-left: 8px;
  transform: translateX(4px);
}

.about-meta span {
  color: var(--sage-dark);
  font-size: 0.75rem;
  padding-top: 3px;
}

.about-meta h3 {
  margin: 0;

  font-family: Georgia, serif;
  font-size: 1.2rem;

  color: var(--mauve-dark);
}

.about-meta p {
  grid-column: 2;
  margin: 0;

  color: var(--text-soft);

  font-size: 0.9rem;
  line-height: 1.6;
}

  @media (max-width: 850px) {
    .about {
      padding: 90px 0;
    }

    .container {
      grid-template-columns: 1fr;
      gap: 50px;
    }

    .about-right {
      padding-top: 0;
    }

    .about-intro {
      grid-template-columns: minmax(0, 1fr) minmax(260px, 340px);
    }

    .about-meta {
      grid-template-columns: 1fr;
    }

    .about-meta > div {
      grid-template-columns: 40px 1fr;
    }

    .about-meta p {
      grid-column: 2;
    }
  }

  @media (max-width: 520px) {
    .container {
      width: min(94%, 1120px);
      gap: 22px;
    }

    .about {
      padding: 75px 0;
    }

    .section-index {
      margin-bottom: 22px;
    }

    h2 {
      width: 96%;
      font-size: clamp(2.65rem, 12.8vw, 3.8rem);
      line-height: 0.98;
      letter-spacing: -1.5px;
    }

    .lead {
      margin-bottom: 18px;
      font-size: clamp(1.2rem, 5.7vw, 1.45rem);
      line-height: 1.5;
    }

    .about-intro {
      grid-template-columns: 1fr;
      gap: 26px;
    }

    .about-photo {
      order: -1;
      width: 72vw;
      max-width: 290px;
      justify-self: end;
      margin-top: -12px;
      margin-right: 4vw;
    }

    .about-photo img {
      border-radius: 18px;
    }

    .about-copy {
      width: 95%;
    }

    .about-copy > p:not(.lead) {
      font-size: 1rem;
      line-height: 1.65;
    }

    .about-meta {
      width: 94%;
      gap: 0;
      margin-top: 10px;
    }

    .about-meta > div {
      grid-template-columns: 34px 1fr;
      column-gap: 12px;
      padding: 17px 0;
    }

    .about-meta h3 {
      font-size: 1.1rem;
    }

    .about-meta p {
      font-size: 0.9rem;
      line-height: 1.55;
    }
  }
</style>
